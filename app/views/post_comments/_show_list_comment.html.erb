<% comments.each do |comment| %>
  <div class="d-flex justify-content-center py-2">
    <div class="second py-2 px-2"> <span class="text7"><%= comment.comment %></span>
      <div class="d-flex justify-content-between py-1 pt-2">
        <span class="text-3"><%= comment.user.name %></span><span class="text-3"><%= comment.decorate.created_at %></span>
      </div>
    </div>
  </div>
<% end %>
